name: Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build project and measure time
        run: |
          echo "🔨 Building with g++..."
          START_TIME=$(date +%s%N)
          make all
          END_TIME=$(date +%s%N)
          BUILD_TIME=$(awk "BEGIN {printf \"%.3f\", ($END_TIME - $START_TIME) / 1000000000}")
          echo "✅ Build completed in ${BUILD_TIME} seconds"
          echo "BUILD_TIME=${BUILD_TIME}" >> $GITHUB_ENV

      - name: Display build time
        run: |
          echo "### 📊 Build Time Summary (g++)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⏱️ **Build Time:** ${BUILD_TIME} seconds" >> $GITHUB_STEP_SUMMARY

      - name: Run benchmark
        run: ./Dragonrose_Cpp bench
